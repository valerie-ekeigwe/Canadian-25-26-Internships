{# =========================
  README (Canada-only Internships)
  Structure: Category -> Discipline -> Level
  Levels: Undergraduate, Graduate
  Status badge: Open (green) / Closed (red)
  ========================= #}

# Canadian Internships — {{ generated_at }}

This list shows **Canada‑only** internships and co‑ops, split by **category, discipline, and level**.  
If a row shows **Closed**, applications are no longer being accepted, but the listing remains for tracking recurring programs.

---

{% for category_name, disciplines in groups.items() %}
## {{ category_name }}

{% for discipline_name, levels in disciplines.items() %}
### {{ discipline_name }}

{% for level_name, postings in levels.items() %}
#### {{ level_name }} Internships (Canada)

| Company | Role | Location | Deadline | Status | Tags | Link |
|--------|------|----------|----------|--------|------|------|
{% if postings|length == 0 %}
| — | — | — | — | — | — | — |
{% else %}
{% for p in postings %}
| {{ p.company }} | {{ p.role }} | {{ p.location or "Canada" }} | {{ p.deadline or "Rolling/unspecified" }} | {% if (p.status or "open")|lower == "open" %}<span style="color:green;font-weight:bold">Open</span>{% else %}<span style="color:red;font-weight:bold">Closed</span>{% endif %} | {{ (p.tags or [])|join(", ") }} | [Apply]({{ p.url }}) |
{% endfor %}
{% endif %}

{% endfor %}
{% endfor %}

---
{% endfor %}

### Legend
- **Level:** Undergraduate = co‑op, internship, bachelor’s student. Graduate = master’s, PhD, EIT, or designated grad internships.
- **Status:** <span style="color:green;font-weight:bold">Open</span> means currently accepting applications; <span style="color:red;font-weight:bold">Closed</span> means not accepting now.

_Last updated: {{ generated_at }}_
fix: match renderer template name

